<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="WineDAO">

	<select id="getWineList" resultType="wine">
		select w_no,w_nm_k,w_nm_e,country,type,w_image1 from wine
	</select>
	
	<select id="WineListAd" resultType="wine">
		select * from wine
	</select>
	
	<select id="checkUser" resultType="int">
		select count(*) from member
		where id = #{id}
	</select>
	
	<insert id="insertUser">
		INSERT INTO member (ID, M_PW, M_NAME, M_PHONE, M_BIRTH, M_EMAIL)
		VALUES (#{id}, #{m_pw}, #{m_name}, #{m_phone}, #{m_birth}, #{m_email})
	</insert>

</mapper>
